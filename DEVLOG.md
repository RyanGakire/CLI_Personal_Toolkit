# Development Progress
## First Attempt
- Declared a variable `nbr`
- Created the menu using `print` and triple-quoted strings.
- Used `if/elif/else` to respond to user choices.
- Added input validation.
- Used `While` loop for handling wrong inputs.

**N3xt goal:**
- Minimizing the code using functions and loops
- Finding ways to handle string inputs
- Adding functionalites/features using functions 
-------------- ----------------- ----------------------
## Second Attempt
- Made the program to end only when the user enters `4`
- Removed the `Search Notes` feature/option

**Next goal:**
- Finding ways of preventing/handling users entering other data types that are not integer
- Finding a way to make the menu appear everytime the loop starts
- Make the user enter his/her notes with `Add Notes` option and store it in a variable for now.
